# Midi Sync Service React Library

This library provides React Components for the Midi Sync Service.

## Running unit tests

Run `nx test services-midi-sync-react` to execute the unit tests via [Jest](https://jestjs.io).
