# Midi Sync Service Contract

This library provides common type definitions for communication between server and client.

## Building

Run `nx build services-midi-sync-contract` to build the library.
